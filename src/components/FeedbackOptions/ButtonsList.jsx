// import ButtonItem from './ButtonItem';

// const ButtonsList = ({ list, onClick, upString }) => {
//   const keys = Object.keys(list);

//   return (
//     <ul>
//       {keys.map((item, index) => (
//         <ButtonItem key={index} onClick={onClick} children={upString(item)} />
//       ))}
//     </ul>
//   );
// };

// export default ButtonsList;
