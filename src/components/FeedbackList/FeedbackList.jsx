// import FeedbackItem from './FeedbackItem';

// const FeedbackList = ({ list, upString, total, percentage }) => {
//   const keys = Object.keys(list);
//   return (
//     <ul>
//       {keys.map((item, index) => (
//         <FeedbackItem
//           children={`${upString(item)}:${list[item]}`}
//           key={index}
//         />
//       ))}
//       <FeedbackItem children={`Total: ${total}`} />
//       <FeedbackItem children={`Positive feedback: ${percentage}%`} />
//     </ul>
//   );
// };

// export default FeedbackList;
